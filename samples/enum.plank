import Std.IO;

type List =
  | Cons(*Char, List)
  | Nil;

fun showList(list: List): *Char {
  return "TODO";
}

fun main(argv: Int, argc: **Char): Void {
  let list = Cons("Yes", Cons("This", Cons("is a linked list", Nil()));

  println(showList(list));
}
